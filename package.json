{
  "name": "gemini-cli-sdk",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "biome check && tsc --noEmit",
    "fix": "biome check --write . && tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "latest"
  }
}
